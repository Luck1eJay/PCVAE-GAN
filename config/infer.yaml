model:
  latent_dim: 64
  checkpoint: checkpoints/stage3/checkpoint_epoch10.pth

data:
  sim_wrap: DATA/TEST/WRAP
  sim_true: DATA/TEST/TRUE
  real_wrap: DATA/REAL

inference:
  num_samples: 8
  batch_size: 1
  device: cuda
  save_dir: outputs


# PCVAE-GAN 项目文件详细说明

本文档详细介绍项目中每个文件的功能和作用（不包括 .idea 文件夹）。

---

## 📁 根目录文件

### README.md
- **功能**: 项目的主说明文档
- **内容**: 包含项目概述、使用方法、安装指南等基本信息
- **用途**: 帮助用户快速了解和使用本项目

### inference.py
- **功能**: 模型推理脚本
- **主要作用**:
  1. 加载训练好的 VAE 模型（从 `checkpoints/vae_stage3.pth`）
  2. 提供两个推理函数：
     - `infer_single(x_wrap)`: 单次推理，使用编码器的均值 mu 作为确定性输出
     - `infer_multi(x_wrap, N=5)`: 多次采样推理，通过重参数化技巧生成 N 个不同的相位解
  3. 支持 CPU 和 GPU 推理
- **输入**: 包裹相位图像 x_wrap
- **输出**: 连续相位 phi 的预测结果
- **使用场景**: 当模型训练完成后，使用此脚本对新的包裹相位数据进行解包裹

---

## 📁 config 文件夹

### pcvae_gan.yaml
- **功能**: 项目的全局配置文件
- **配置内容**:
  1. **data**: 数据路径配置
     - `sim_path`: 模拟数据路径（包含 wrap + phi 配对数据）
     - `real_path`: 真实数据路径（只有 wrap 数据）
  2. **model**: 模型架构参数
     - `z_dim`: VAE 隐变量维度（默认 32）
     - `encoder_channels`: 编码器卷积通道数列表
     - `decoder_channels`: 解码器卷积通道数列表
  3. **train**: 训练超参数
     - `batch_size`: 批次大小
     - `lr_vae`, `lr_disc`: VAE 和判别器的学习率
     - `epochs_stage1/2/3`: 三个训练阶段的轮数
     - `kl_weight`, `gan_weight`, `diversity_weight`: 各损失权重
  4. **loss**: 损失函数权重
     - `sup_weight`: 监督损失权重
     - `wrap_weight`: 包裹一致性损失权重
- **作用**: 集中管理所有可调参数，方便实验调参

---

## 📁 data 文件夹

### sim_dataset.py
- **功能**: 模拟数据的 PyTorch Dataset 类
- **数据特点**: 
  - 模拟数据包含**配对的** wrap 和 phi_gt（真实连续相位）
  - 用于**有监督训练**
- **实现细节**:
  - `__init__`: 加载数据目录，获取文件列表
  - `__len__`: 返回数据集大小
  - `__getitem__`: 读取 `.npy` 文件，返回 (x_wrap, phi_gt) 元组
- **数据格式**: NumPy 数组，shape 为 (2, H, W)，其中第 0 通道是 wrap，第 1 通道是 phi_gt
- **使用场景**: Stage 1 预训练阶段使用

### real_dataset.py
- **功能**: 真实数据的 PyTorch Dataset 类
- **数据特点**:
  - 真实数据**只有 wrap**，没有真实标签 phi_gt
  - 用于**无监督或半监督训练**
- **实现细节**:
  - `__init__`: 加载数据目录，获取文件列表
  - `__len__`: 返回数据集大小
  - `__getitem__`: 读取 `.npy` 文件，只返回 x_wrap
- **数据格式**: NumPy 数组，shape 为 (H, W)
- **使用场景**: Stage 2（GAN 对齐）和 Stage 3（多解训练）使用

---

## 📁 losses 文件夹

### sup_loss.py
- **功能**: 监督损失函数
- **数学公式**: `L_sup = MSE(phi_pred, phi_gt) = mean((phi_pred - phi_gt)²)`
- **作用**: 在有标签的模拟数据上，直接监督模型预测的连续相位与真实值的接近程度
- **使用场景**: Stage 1 预训练

### wrap_loss.py
- **功能**: 包裹一致性损失
- **关键函数**:
  1. `wrap_phase(phi)`: 将连续相位包裹到 [-π, π] 范围
     - 公式: `wrap(phi) = atan2(sin(phi), cos(phi))`
  2. `wrap_loss(phi_pred, x_wrap)`: 计算包裹一致性损失
     - 公式: `L_wrap = mean(|wrap(phi_pred) - x_wrap|)`
- **物理意义**: 确保模型预测的连续相位，经过包裹后能还原为输入的包裹相位
- **作用**: 这是一个关键的自监督约束，即使没有真实标签也能使用
- **使用场景**: 所有训练阶段

### kl_loss.py
- **功能**: KL 散度损失（VAE 正则项）
- **数学公式**: `KL(q(z|x) || N(0,I)) = -0.5 * mean(1 + log(σ²) - μ² - σ²)`
- **作用**: 
  1. 正则化隐变量分布，使其接近标准正态分布
  2. 防止模型过拟合
  3. 使隐空间更加平滑连续
- **使用场景**: 所有训练阶段

### gan_loss.py
- **功能**: GAN 训练的对抗损失
- **包含函数**:
  1. `gan_loss_D(pred_sim, pred_real)`: 判别器损失
     - 目标: 让判别器能区分模拟数据和真实数据的隐变量
     - 公式: `L_D = BCE(pred_sim, 1) + BCE(pred_real, 0)`
  2. `gan_loss_G(pred_real)`: 生成器（编码器）损失
     - 目标: 让编码器生成的真实数据隐变量骗过判别器
     - 公式: `L_G = BCE(pred_real, 1)`
- **作用**: 通过对抗训练，使真实数据的隐变量分布与模拟数据对齐
- **使用场景**: Stage 2 隐空间对齐训练

### diversity_loss.py
- **功能**: 多解多样性损失
- **数学公式**: `L_div = -mean(|phi_i - phi_j|)` (对所有解对 i,j)
- **作用**: 
  - 负号表示**最大化**解之间的差异
  - 鼓励模型对同一个输入生成多个不同的合理解
  - 解决相位解包裹的多解问题
- **使用场景**: Stage 3 多解训练

---

## 📁 models 文件夹

### encoder.py
- **功能**: VAE 编码器网络
- **网络结构**:
  1. 卷积层序列: 逐步下采样输入图像
  2. 全局自适应平均池化: 使模型对输入尺寸更鲁棒
  3. 全连接层: 输出 mu（均值）和 logvar（对数方差）
- **输入**: 包裹相位图像 x_wrap，shape 为 (B, C, H, W)
- **输出**: 隐变量分布参数 (mu, logvar)
- **设计特点**:
  - 使用自适应池化避免对输入尺寸的硬编码
  - 通道数可配置
  - 输出两个参数用于 VAE 的重参数化技巧
- **作用**: 将包裹相位图像编码到低维隐空间

### decoder.py
- **功能**: VAE 解码器网络
- **网络结构**:
  1. 全连接层: 将隐变量 z 映射到初始特征图
  2. 转置卷积层序列: 逐步上采样恢复图像分辨率
- **输入**: 
  - x_wrap: 包裹相位（保留接口，但当前实现未使用）
  - z: 隐变量
- **输出**: 连续相位预测 phi
- **设计特点**:
  - 使用转置卷积进行上采样
  - 通道数可配置
  - 保留 x_wrap 参数以兼容未来可能的条件解码
- **作用**: 从隐变量 z 重建连续相位

### discriminator.py
- **功能**: 隐空间判别器
- **网络结构**: 简单的 MLP（多层感知机）
  - 输入层: z_dim 维
  - 隐藏层: 64 维 + ReLU
  - 输出层: 1 维（真/假分数）
- **输入**: 隐变量 z
- **输出**: 标量分数（经过 sigmoid 后表示"真实数据"的概率）
- **作用**: 在 Stage 2 中判别隐变量来自模拟数据还是真实数据
- **训练目标**: 
  - 判别器学习区分两种数据
  - 编码器学习生成相似的隐变量分布

### vae_model.py
- **功能**: 完整的 VAE 模型
- **组成**: 组合 Encoder 和 Decoder
- **关键方法**:
  1. `__init__`: 初始化编码器和解码器
  2. `forward(x_wrap)`: 完整的前向传播
     - 编码: x_wrap → (mu, logvar)
     - 重参数化: (mu, logvar) → z
     - 解码: (x_wrap, z) → phi_hat
     - 返回: (phi_hat, mu, logvar, z)
  3. `reparameterize(mu, logvar)`: 重参数化采样
     - 实现: z = mu + eps * exp(0.5 * logvar)
     - 作用: 使采样过程可微分
- **作用**: 封装完整的 VAE 架构，便于训练和推理

---

## 📁 train 文件夹

### stage1_pretrain_sim.py
- **功能**: 第一阶段训练脚本 - 在模拟数据上预训练
- **训练目标**: 学习基本的相位解包裹能力
- **数据**: 使用 SimDataset（有配对标签）
- **训练流程**:
  1. 加载配置和模拟数据
  2. 初始化 VAE 模型和优化器
  3. 对每个 batch:
     - 前向传播: x_wrap → phi_hat
     - 计算损失:
       - `L_sup`: 监督损失（与真实 phi_gt 对比）
       - `L_wrap`: 包裹一致性损失
       - `L_kl`: KL 散度正则
     - 反向传播更新参数
  4. 每 10 个 epoch 保存一次模型
- **损失函数**: `Loss = sup_weight * L_sup + wrap_weight * L_wrap + kl_weight * L_kl`
- **输出**: `checkpoints/vae_stage1.pth`
- **重要性**: 这是整个训练的基础，模型在此阶段学会基本的解包裹能力

### stage2_real_latentGAN.py
- **功能**: 第二阶段训练脚本 - 隐空间 GAN 对齐
- **训练目标**: 使真实数据的隐变量分布与模拟数据对齐
- **数据**: 同时使用 SimDataset 和 RealDataset
- **关键设置**:
  - 加载 Stage1 预训练权重
  - **冻结解码器**（非常重要！保持已学会的解包裹能力）
  - 只训练编码器和判别器
- **训练流程**:
  1. 加载预训练模型和数据
  2. 对每个 batch:
     - 训练判别器 D:
       - 目标: 区分 z_sim（模拟）和 z_real（真实）
       - 损失: `L_D = BCE(D(z_sim), 1) + BCE(D(z_real), 0)`
     - 训练编码器 E（作为生成器）:
       - 目标: 让 z_real 骗过判别器
       - 损失: `L_G = BCE(D(z_real), 1)`
  3. 保存更新后的模型
- **输出**: `checkpoints/vae_stage2.pth`, `checkpoints/disc_stage2.pth`
- **作用**: 通过对抗训练，使模型能够处理真实数据

### stage3_multi_sample.py
- **功能**: 第三阶段训练脚本 - 多解训练
- **训练目标**: 学习生成多个不同但合理的相位解
- **数据**: 只使用 RealDataset（无标签）
- **训练流程**:
  1. 加载 Stage2 模型
  2. 对每个输入，采样多个隐变量（N_samples=4）:
     - 编码: x_wrap → (mu, logvar)
     - 多次采样: z_1, z_2, ..., z_N ~ q(z|x)
     - 多次解码: phi_1, phi_2, ..., phi_N
  3. 计算损失:
     - `L_wrap`: 所有解都要满足包裹一致性
     - `L_div`: 多样性损失（鼓励解之间不同）
     - `L_kl`: KL 正则
  4. 反向传播更新整个 VAE
- **损失函数**: `Loss = wrap_weight * L_wrap + kl_weight * L_kl + diversity_weight * L_div`
- **输出**: `checkpoints/vae_stage3.pth`
- **重要性**: 解决相位解包裹的多解问题，使模型能够输出多个合理的解

---

## 📁 utils 文件夹

### utils.py
- **功能**: 工具函数集合
- **包含函数**:

  1. **reparameterize(mu, logvar)**
     - 功能: VAE 的重参数化技巧
     - 公式: `z = mu + eps * exp(0.5 * logvar)`，其中 eps ~ N(0,1)
     - 作用: 使采样操作可导，允许梯度反向传播
  
  2. **wrap_phase(phi)**
     - 功能: 将连续相位包裹到 [-π, π]
     - 公式: `wrap(phi) = atan2(sin(phi), cos(phi))`
     - 作用: 模拟实际测量中的相位包裹现象
  
  3. **unwarp_phase(phi_wrap, phi_prev)**
     - 功能: 简单的相位解包裹（示例实现）
     - 原理: 通过前一点的相位值，估计当前点的跳变
     - 作用: 提供基础的相位解包裹功能
  
  4. **save_model(model, path)**
     - 功能: 保存模型权重
     - 实现: 使用 `torch.save` 保存 state_dict
  
  5. **load_model(model, path, device)**
     - 功能: 加载模型权重
     - 特点: 支持跨设备加载（CPU/GPU）
  
  6. **load_cfg(yaml_path)**
     - 功能: 加载 YAML 配置文件
     - 返回: Python 字典格式的配置
  
  7. **sample_multiple_solutions(vae, x_wrap, N)**
     - 功能: 从 VAE 采样多个解
     - 流程: 编码 → 多次采样 z → 多次解码
     - 返回: N 个不同的相位预测

---

## 🎯 项目整体工作流程

1. **Stage 1 - 预训练**: 在模拟数据上学习基本解包裹能力
2. **Stage 2 - 域对齐**: 通过 GAN 使模型适应真实数据
3. **Stage 3 - 多解训练**: 学习生成多个不同的合理解
4. **推理**: 使用训练好的模型对新数据进行解包裹

---

## 📊 总结

本项目实现了基于 PCVAE-GAN 的相位解包裹方法，通过三阶段训练策略：
- 解决了**有监督到无监督**的迁移问题（Stage 1 → 2）
- 解决了**单解到多解**的扩展问题（Stage 2 → 3）
- 实现了端到端的相位解包裹系统

每个文件都有明确的职责，代码结构清晰，易于理解和扩展。
